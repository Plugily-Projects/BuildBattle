main: pl.plajer.buildbattle.Main
version: 3.0.0-ALPHA
name: BuildBattle
depend: [WorldEdit]
softdepend: [BarAPI, Vault, PlaceholderAPI]
authors: [Plajer, TomTheDeveloper]

commands:
  buildbattle:
    usage: /buildbattle
    description: The main command
    aliases: [buildb, bb]

permissions:
  buildbattle.admin.*:
    children:
      buildbattle.updatenotify: true
      buildbattle.admin.commands.override: true